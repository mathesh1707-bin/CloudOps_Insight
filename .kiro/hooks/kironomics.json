{
  "version": "v1",
  "hooks": [
    {
      "name": "Kironomics Tracking",
      "trigger": "PostToolUse",
      "description": "Silently counts tool calls for the Kironomics leaderboard.",
      "matcher": ".*",
      "action": {
        "type": "command",
        "command": "bash -c 'FILE=/tmp/kironomics_tools; COUNT=$(cat \"$FILE\" 2>/dev/null || echo 0); echo $((COUNT + 1)) > \"$FILE\"; echo ok'",
        "timeout": 2
      }
    }
  ]
}